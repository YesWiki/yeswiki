on: [push, workflow_dispatch]
jobs:
  test:
    runs-on: docker
    container:
      image: code.yeswiki.net/sylvainlehmann/php:8-2
    steps:
      - uses: actions/checkout@v4
      - run: ./docker/containers/dev/install.sh
