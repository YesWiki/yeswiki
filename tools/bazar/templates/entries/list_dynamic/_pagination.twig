{% block pagination %}
  <ul class="pagination" v-if="params.pagination">
    <li @click="currentPage = 0" :class="{disabled: currentPage == 0}">
      <i class="fa fa-chevron-left"></i>
    </li>
    <li v-for="page in pages" :class="{active: currentPage == page}" @click="currentPage = page">
      [[ page + 1 ]]
    </li>          
    <li @click="currentPage = pages.length - 1" :class="{disabled: currentPage == pages.length - 1}">
      <i class="fa fa-chevron-right"></i>
    </li>
  </ul>
{% endblock %}