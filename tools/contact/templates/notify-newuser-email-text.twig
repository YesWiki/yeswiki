{{ _t('CONTACT_NEW_USER_MESSAGE')|replace({
    '{{ baseUrl }}': baseUrl,
    '{{ wikiName }}': wikiName,
    '{{ email }}': email,
    '{{ urlForPasswordRenewal }}': url({tag:'MotDePassePerdu'}),
})|raw }}


