{% from '@attach/display-image-macro.twig' import displayImage %}

{{ displayImage(baseUrl, imageFullPath, fieldName, thumbnailHeight, thumbnailWidth, imageHeight, imageWidth, class ?? "",mode ?? "fit", showThumbnail ?? true, shortImageName ?? "") }}