{# Ticket 16: a boosted navigation returns the body block as a fragment, and htmx applies a
   fragment's own top-level <title> to the document. This is that element -- it must stay a
   direct child of the response, and must not be wrapped in a <head>, which htmx strips. #}
<title>{{ title }}</title>
