{% extends "@core/dashboard/layout.twig" %}

{% block dashboard_content %}
  <h1>{{ _t('IMPORTER_ADMIN_TITLE') }}</h1>

  <section class="yw-dashboard__card yw-dashboard__card--wide">
    {{ renderAction('adminimporters')|raw }}
  </section>

  <section class="yw-dashboard__card yw-dashboard__card--wide">
    <h2>{{ _t('IMPORTER_SYNC_ALL') }}</h2>
    {{ renderAction('sync')|raw }}
  </section>
{% endblock %}
