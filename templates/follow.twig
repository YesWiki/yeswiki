<strong>{{ _t('BAZ_FOLLOW_FORM_ACTOR')|replace({'{actorName}':form['label']}) }}</strong>
<form action="" method="post" class="">
  <input name="form_id" type="hidden" value="{{ form['id'] }}" />
	<div class="input-prepend input-append input-group">
		<span class="add-on input-group-addon">
			<i class="fa fa-link icon-globe"></i>
		</span>
		<input name="actor_handle" class="yw-input input-xxlarge" placeholder="{{ _t('BAZ_FOLLOW_ACTOR_INFO') }}" />
		<span class="input-group-btn">
			<button class="yw-btn yw-btn--primary" type="submit">
				{{ _t('BAZ_GO') }}&nbsp;!
			</button>
		</span>
	</div>
</form>