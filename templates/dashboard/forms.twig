{% extends "@core/dashboard/layout.twig" %}

{% block dashboard_content %}
  <h1>{{ _t('DASHBOARD_FORMS') }}</h1>
  <p class="yw-dashboard__intro">{{ _t('DASHBOARD_FORMS_INTRO') }}</p>
  {{ renderAction('bazar', { showmenu: '0' })|raw }}
{% endblock %}
