{% extends "@core/dashboard/layout.twig" %}

{# BazaR's lists: the shared vocabularies a form's checkbox, radio and select fields draw
   their options from. Its own tab bar is off -- see dashboard/forms.twig. #}
{% block dashboard_content %}
  <h1>{{ _t('DASHBOARD_ADMIN_LISTS') }}</h1>
  {{ renderAction('bazar', { view: 'listes', showmenu: '0' })|raw }}
{% endblock %}
