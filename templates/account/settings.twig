{% extends "@core/dashboard/layout.twig" %}

{# Your account. `{{usersettings}}` is the same action `ParametresUtilisateur` carried: the
   e-mail, the password and the way out, for the person whose session this is. #}
{% block dashboard_content %}
  <h1>{{ _t('ACCOUNT_TITLE') }}</h1>
  {{ renderAction('usersettings')|raw }}
{% endblock %}
