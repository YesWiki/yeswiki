{% extends "@core/account/guest-layout.twig" %}

{% block dashboard_content %}
  {{ renderAction('lostpassword')|raw }}
  <p class="yw-account__aside">
    <a href="{{ url({ tag: 'user' }) }}">{{ _t('ACCOUNT_BACK_TO_LOGIN') }}</a>
  </p>
{% endblock %}
