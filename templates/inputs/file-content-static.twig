
<div class="BAZ_champ file-content">
    {% if label %}<span class="BAZ_label">{{ label }}</span>{% endif %}
    <a href="{{ downloadUrl }}" target="_blank" download="{{ filename }}">
        {{ icon('file') }} {{ filename }}
    </a>
</div>
