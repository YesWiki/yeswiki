
<a class="navbar-brand yw-brand yw-brand--{{ mode }}" href="{{ home }}">
  {% if mode != 'text' and logo %}

    <img class="yw-brand__logo" src="{{ logo }}" alt="{{ title }}">
  {% endif %}
  {% if mode != 'logo' %}
    <span class="yw-brand__title">{{ title }}</span>
  {% endif %}
</a>
