{% if message %}
<div class="yw-alert yw-alert--success">
  <a data-yw-dismiss="alert" class="yw-close" type="button">&times;</a>
  {{ _t(message) }}
</div>
{% endif %}

<div class="yw-alert yw-alert--info">
<strong>{{ _t('BAZ_FORM_ACTOR_ID') }}</strong>: @{{ form['activitypub_username'] }}@{{ domain }}
</div>

<strong>{{ _t('BAZ_FOLLOW_ACTOR') }}</strong>
<form action="" method="post" class="">
	<div class="input-prepend input-append input-group">
		<span class="add-on input-group-addon">
			<svg class="yw-icon icon-globe" aria-hidden="true"><use href="src/assets/icons.svg#link"/></svg>
		</span>
		<input name="actor_handle" class="yw-input input-xxlarge" placeholder="{{ _t('BAZ_FOLLOW_ACTOR_INFO') }}" />
		<span class="input-group-btn">
			<button class="yw-btn yw-btn--primary" type="submit">
				{{ _t('BAZ_GO') }}&nbsp;!
			</button>
		</span>
	</div>
</form>

<table id="existing-lists-table" class="table table-condensed table-striped">
	<thead>
		<tr>
			<th>{{ _t('BAZ_ABONNEMENTS') }}</th>
			<th style="width:140px;">{{ _t('BAZ_ACTIONS') }}</th>
		</tr>
	</thead>
	<tbody>
	{% for actorUri in following %}
		<tr>
			<td>
				<a href="{{ actorUri }}" target="_blank">{{ actorUri }}</a>
			</td>
			<td>
				<a
					class="btn btn-default btn-xs"
					href="{{ url({ params: { vue: 'abonnements', action: 'sync', actor: actorUri, idformulaire: form.id }}) }}"
					title="{{ _t('BAZ_SYNC_ACTOR_POSTS') }}"
				>
					<svg class="yw-icon icon-white" aria-hidden="true"><use href="src/assets/icons.svg#repeat"/></svg>
				</a>
				<a
					class="btn btn-danger btn-xs"
					href="{{ url({ params: { vue: 'abonnements', action: 'remove', actor: actorUri, type: 'following', idformulaire: form.id }}) }}"
					onclick="javascript:return confirm('{{ _t('BAZ_CONFIRM_SUPPRIMER_ABONNEMENT') }} ?');"
				>
					<svg class="yw-icon icon-white" aria-hidden="true"><use href="src/assets/icons.svg#trash"/></svg>
				</a>
		</td>
	</tr>
	{% endfor %}
	</tbody>
</table>

<table id="existing-lists-table" class="table table-condensed table-striped">
	<thead>
		<tr>
			<th>{{ _t('BAZ_ABONNES') }}</th>
			<th style="width:140px;">{{ _t('BAZ_ACTIONS') }}</th>
		</tr>
	</thead>
	<tbody>
	{% for actorUri in followers %}
		<tr>
			<td>
				<a href="{{ actorUri }}" target="_blank">{{ actorUri }}</a>
			</td>
			<td>
				{% if actorUri not in following %}
				<a
					class="btn btn-default btn-mini btn-xs"
					href="{{ url({ params: { vue: 'abonnements', action: 'add', actor: actorUri, type: 'following', idformulaire: form.id }}) }}"
					title="{{ _t('BAZ_FOLLOW_BACK') }}"
				>
					<svg class="yw-icon icon-white" aria-hidden="true"><use href="src/assets/icons.svg#user-plus"/></svg>
				</a>
				{% endif %}
				<a
					class="btn btn-danger btn-xs"
					href="{{ url({ params: { vue: 'abonnements', action: 'remove', actor: actorUri, type: 'followers', idformulaire: form.id }}) }}"
					onclick="javascript:return confirm('{{ _t('BAZ_CONFIRM_SUPPRIMER_ABONNE') }} ?');"
				>
					<svg class="yw-icon icon-white" aria-hidden="true"><use href="src/assets/icons.svg#trash"/></svg>
				</a>
		</td>
	</tr>
	{% endfor %}
	</tbody>
</table>

