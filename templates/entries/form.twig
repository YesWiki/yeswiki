
{{ include_css('styles/yw-entries.css') }}
{{ include_javascript('javascripts/bazar.js', true, true) }}
{{ error|raw }}
<h3 class="titre_type_fiche">{{ _t('BAZ_TITRE_SAISIE_FICHE') }}&nbsp;{{ form.label }}</h3>

<div class="yw-designer">
<div class="yw-designer__canvas">

<form action="" method="post" name="formulaire" id="formulaire" enctype="multipart/form-data" hx-boost="false" novalidate>
  {% for renderedInput in renderedInputs %}
    {{ renderedInput|raw }}
  {% endfor %}
  {{ captchaField|raw }}
  <div class="form-actions yw-form-group">
    <div class="col-sm-9 col-sm-offset-3">
      <button type="submit" name="valider" value="1" class="yw-btn yw-btn--primary">{{ _t('BAZ_VALIDER') }}</button>
      <a class="yw-btn yw-btn--sm" onclick="javascript:history.back()">{{ _t('BAZ_ANNULER') }}</a>
    </div>
  </div>
  <input name="antispam" value="0" type="hidden">
  <input name="form_id" value="{{ form.id }}" type="hidden">
  {% if entryId %}
    <input name="tag" value="{{ entryId }}" type="hidden">
  {% endif %}
  {% if passwordForEditing %}
    <input name="password_for_editing" value="{{ passwordForEditing }}" type="hidden">
  {% endif %}
  {% if incomingUrl %}
    <input name="incomingurl" value="{{ incomingUrl }}" type="hidden">
  {% endif %}
</form>

</div>
{{ include('@core/aceditor-rails.twig') }}
</div>
