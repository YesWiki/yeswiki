# YesWiki (ectoplasme rewrite)

YesWiki is a wiki engine. The `ectoplasme` branch is its next MAJOR version: a standardization rewrite that folds most bundled `tools/` into core services/actions/handlers and unifies the data model around a single `pages` table.

## Language

**Content**:
Anything stored as a row in the `pages` table: an ordinary wiki page, a form definition (formerly the `nature` table), or a user account (formerly the `users` table). Distinguished by a triple `(resource=tag, property=TripleStore::TYPE_URI, value=<type>)` in the `triples` table — the existing mechanism already used to mark bazar entries (`value='fiche_bazar'`, see `EntryManager::TRIPLES_ENTRY_ID`) and extended by [PR #1333](https://github.com/YesWiki/yeswiki/pull/1333) to mark forms; there is no `type` column on `pages` itself.
_Avoid_: Page (when the type could be a form or user), record, entity, "type column" (it's a triple, not a column).

**Content type**:
Which kind of Content a row is: `entry`, `page`, `user`, `file` (plus `liste`). Declared per row by the `TripleStore::TYPE_URI` triple, and named on a form's body by `content_type` (`ContentTypeSchema`) to say which core structure that form describes. Page, User and File are forms like any other — same designer, same template, same storage (ADR-0011). A row of an _untyped_ page carries no triple at all: being untyped is what makes it a page, which is how a search selects the Page form's rows (they carry no `form_id` either). The type also decides how a Content of it is named and which Form properties apply to it.
_Avoid_: "nature" (the retired table), "kind", treating `entry` as the only type with a template.

**Locked field**:
A field in a Content type's mandatory core structure — Page's `title`/`content`/`keywords`, User's `username`/`password`/`email`/`profile_picture`, File's `file_content` (its bytes — the attributes derived from an upload are body keys, not inputs). It cannot be deleted or retyped; its label, help text, order and Field ACL are ordinary and editable, and webmaster-added fields sit beside it in the same list. Locked-ness is declared in `ContentTypeSchema`, never stored as an attribute on the field — a stored flag would be clearable through the very write vectors it has to survive (ADR-0011).
A type may declare one of them as its **tag mirror** (User's `username`): its value _is_ the row's tag, filled in on read rather than stored twice, and not offered as an input. A type with no `content` field has no wiki markup at all — the editor offers it none, and writes none.
_Avoid_: "system field", "read-only field" (only deletion and retyping are barred), "required field" (that is the separate `required` attribute a visitor's input must satisfy).

**Tag**:
The globally unique identifier of a piece of Content, regardless of its type. A wiki page, a form, and a user account all draw from the same tag namespace — creating a user named `JohnDoe` is only possible if no page or form already holds that tag. Helper functions resolve collisions by suggesting an alternative tag rather than silently namespacing by type. _Generated_ tags (bazar entries, new forms) are lowercase slugs (`l-ete-a-nantes`, collisions suffixed `-2`); _user-chosen_ tags (usernames, hand-created pages) are kept exactly as typed — usernames are never slugified. Existing tags are never rewritten.
_Avoid_: Name, key, CamelCase id generation (`generateWikiName` is retired for generated tags).

**Body**:
The `body` column on each `pages` row: **a JSON object for every Content type** (ticket 09). It holds what the Content _is_ — its own data, including every webmaster-added field. A wiki page and a comment carry their wiki markup as a string under `content`; a bazar entry carries its form fields at the top level; a form carries its `template` and Form properties. The container is uniform, the keys are per-type. Encoded and decoded through `PageBody` (`src/Content/Entity/PageBody.php`), never with a bare `json_encode`. The column is the dialect's **native** JSON type where there is one — `JSON`, `JSONB`, and `TEXT` on SQLite, which has none (ADR-0018). It was text until then to keep a MySQL FULLTEXT index possible; ticket 18 moved search to its own index table and removed that constraint, and the column type was measured rather than assumed. Read it by path with `DbService::jsonExtract()` and never with a string operator: `jsonb` has no `LIKE`, and `jsonAsText()` is what a genuine free-text search over a whole body uses.
_Avoid_: "the markup" (only pages and comments have any), sniffing the shape from `body[0] === '{'` (83 pages in the reference wiki open with an action call — the type comes from the `TYPE_URI` triple).

**Metadata**:
A JSON column on each `pages` row holding **how Content is presented and who may see it**, and nothing else: `acls` (replacing the `acls` table), `theme`, `squelette`, `style`, `favorite_preset`, `bgimg`, `lang`. Versioned along with the row — reverting Content to a prior revision reverts its Metadata too. Stored in the dialect's native JSON type alongside `body` (ADR-0018), and **nullable** — a row with no metadata at all is normal, and stays NULL. Absorbs and replaces the pre-existing, non-versioned `AdminContentController::METADATA_PROPERTY` triple (`http://outils-reseaux.org/_vocabulary/metadata`) — that older mechanism is retired, not kept alongside the new column. Anything a webmaster can add, rename or delete is a Body field, not Metadata; ticket 09 moved file attributes and ActivityPub settings out of Metadata on that rule (see ADR-0002's amendment).
_Avoid_: ACLs (too narrow — ACLs are one key within Metadata, not the whole column), properties, attributes, the old `METADATA_PROPERTY` triple (retired), using it as a home for data fields.

**`yw-*` core styles**:
The minimalist CSS/JS design system shipped by core after Bootstrap is dropped, namespaced under a `yw-*` class prefix so it can't collide with whatever a theme brings in. Replaces per-theme Bootstrap dependence; existing themes (including `yeswiki-theme-bootstrap3`) are expected to be retired rather than ported, and new themes are CSS-framework-agnostic — free to use any framework or none, since core no longer imposes one. Its interactive behavior is built on **vanilla JS and htmx only** — no jQuery, no Bootstrap JS, no other JS framework, starting with this major release.
_Avoid_: Bootstrap classes (retired from core; a small legacy vocabulary present in stored wiki content stays styled by core — see ADR-0004's ticket-16 amendment — but is not an API for new code), jQuery (retired from core; one disclosed island remains, see ADR-0005's ticket-16 amendment and ticket 26), theme framework (themes have none imposed).

**Design token**:
A named `--yw-*` CSS custom property that core's own rules consume — the only vocabulary they may consume. Two tiers, and the distinction is the whole point: an **authored token** is a decision a **Preset** states (`--yw-primary`, `--yw-surface`, `--yw-navbar-bg`, `--yw-space-md`), a **derived token** is computed by core from an authored one and may not be stated by a Preset (`--yw-primary-hover`, `--yw-text-muted`, `--yw-success-surface`, `--yw-radius-md`). Rules reference roles either way, never raw colours, which is what lets a Colour scheme be a swap rather than a rewrite — and because a derived token re-resolves against whatever the scheme last said, one authored colour is correct in both schemes (ADR-0021). A **measure** is authored as a multiple of the base type size, never as a typed length. Distinct from a **Layout setting**: if an admin screen writes it, it is not a token.
_Avoid_: the pre-`yw` variable names (`--primary-color`, `--neutral-soft-color`, `--main-bg-color` and the rest — retired, not aliased), "CSS variable" when a token is meant (a token is a declared part of the theming API; not every `--yw-*` property is one), hardcoded colours and lengths in core rules.

**Layout setting**:
A `--yw-*` custom property an admin screen owns, written **inline on the root element** so it beats the same property declared in any stylesheet — preset, theme or custom CSS — with no `!important` anywhere (`--yw-navbar-height`, ticket 30). Looks exactly like a **Design token** and is governed by the opposite rule: a Preset may neither declare it nor be validated against it, because such a declaration could never take effect.
_Avoid_: calling one a token, letting a Preset and an admin screen write the same property (one writer each, always).

**Preset**:
A complete set of Design token values, authored as one CSS file and chosen **per page** (a `favorite_preset` key in Metadata, falling back to a wiki-wide default). A Preset covers the whole look — colour, spacing rhythm and corner shape — not colour alone, so a "compact" or "rounded" Preset is expressible. Complete by rule: it declares every token, colour tokens once per Colour scheme and the scheme-independent ones once, and core validates that rather than filling gaps. Nothing about a Preset is computed or inherited from another Preset.
_Avoid_: theme (a theme brings templates and layout; a Preset is only token values), "palette" for the file, partial or patch presets (an incomplete Preset is an error, not a shorthand).

**Colour scheme**:
Light or dark, chosen by the **viewer** — from their OS preference by default, overridable by an explicit toggle. Orthogonal to Preset, which is chosen by the page: a page pins which Preset applies, a viewer picks which of that Preset's two token sets is used. Three states, not two: system-follows, forced light, forced dark.
_Avoid_: "dark theme" / "dark preset" (it is neither a theme nor a Preset — conflating the two makes a page's choice fight the viewer's), treating system-default as a fourth Preset.

**Field ACL**:
A per-field (not per-page) read/write access-control list, using the same ACL syntax as page-level Metadata ACLs. Already implemented for bazar entry fields (`read_access`/`write_access` attributes, `src/Content/Field/BazarField.php`, enforced by `canRead()`/`canEdit()`); this rewrite extends it to `users`-type Content, where it hides sensitive fields (e.g. the hashed password) that otherwise live in the same versioned `body` as everything else, rather than carving those fields into a separate non-versioned table.
_Avoid_: Private field, restricted field (both used informally in existing bazar code/UI, but "Field ACL" is the precise mechanism name).

**Form template**:
The JSON array of field objects stored under `template` in a form's body — the schema of the inputs filled when creating that form's Content, and nothing else. Since ticket 10 that Content may be a page, a user or a file as well as an entry, and some of its fields may be locked (see **Locked field**) — what has _not_ widened is what a template is for. Attribute keys are the `FIELD_*` constant names of the handling field class. Form-level behavior (title computation, entry ACLs, presentation, account creation, bookmarklet) never lives here — those are Form properties.
_Avoid_: `***` syntax (legacy, read-only), `bn_template` (renamed), "prepared json" (ambiguous — see Prepared fields), pseudo-field / special field (retired concept).

**Form property**:
A named key in a form's body holding form-level configuration: identity (`id`, `label`, `description`, `lang`), behavior (`entry_title_template`, `entry_read_access`, `entry_write_access`, `entry_comment_access`, `entry_permit_activate_comments`, `entry_creates_user`, `entry_bookmarklet`), presentation (`entry_metadatas`), and the legacy-carried `sem_*`, `only_one_entry*`, `activitypub_*`, `condition`. Plain-English names; the `bn_` prefix is retired. Which ones apply depends on the Content type: `entry_creates_user` and `entry_bookmarklet` describe visitor submissions, so a built-in type drops them (on read as well as on write) and the designer does not offer them; `entry_title_template` defaults to one of the type's own locked fields rather than to the bazar `{{bf_titre}}` convention.
_Avoid_: `bn_*` keys (renamed), pseudo-fields (form behavior stored as fake template fields — retired).

**Content title**:
The name a Content goes by in a list, a link or a search result. Computed from its form's `entry_title_template` (ADR-0010) and stored as `title` in the body. A `{{field}}` with no value substitutes to nothing rather than being left standing, and an empty result falls back to the row's **tag** — every Content has a name, and an unresolved placeholder is not one.
_Avoid_: `bf_titre` (a bazar field name, not the concept), showing a raw `{{...}}` to a visitor.

**Field role**:
Core's question about a form's fields — "which one holds the start date?" — answered by the form rather than by a hardcoded field name. Roles: `start_date`, `end_date`, `image`, `email`, `description`, `geolocation`. Resolved through `FieldRoleResolver`, from the form's explicit `field_roles` property when it has one and otherwise from the field's own type (a `listedatedeb` field is the start date), so existing forms need no migration. Generalises ticket 27's `entry_title_template` (ADR-0012).
_Avoid_: reading `bf_date_debut_evenement`/`bf_latitude`/`bf_titre` or any literal field name from core — field names are user data; "magic field name" is the retired anti-pattern.

**Prepared fields**:
The runtime field objects constructed from the Form template (one per real field), serialized in the API as `prepared` — what rendering consumers iterate. The internal positional arrays feeding the field constructors are an implementation detail and are not exposed anywhere.
_Avoid_: "prepared json" for the _stored_ template (the stored template is the Form template; `prepared` is derived from it), positional template arrays in any public payload.

**Entry title**:
The `title` key of an entry, always computed at save from the form's `entry_title_template` (a `{{field}}` substitution template; picking a single field stores `{{bf_x}}`). Also the source for the entry's generated tag (slug).
_Avoid_: `bf_titre` as the title mechanism (it may exist as an ordinary input field a template references, but nothing reads it as "the title" anymore).

**Module**:
One of the six top-level boundaries the code is divided into: `Kernel` (infrastructure, depended upon by everything, depending on no feature) plus the features `Content`, `Identity`, `Render`, `Search` and `Admin`. A module is a boundary, not a folder: the rules that define it — no service depends on a controller, every `/api/*` route lives in its owning module's `Api` namespace — are enforced by a test, so a breach appears as an import in a diff rather than as one more class in a crowded directory (ADR-0013).
_Avoid_: layer (the retired `services`/`controllers`/`fields` split, which is what made a service depending on a controller look normal), package, namespace (a module has one, but the namespace is the consequence, not the concept).

**Declared assets**:
The CSS and JavaScript a render says it needs, returned together with what was rendered rather than accumulated in a global for a footer to flush. A whole page and a single field's markup declare theirs the same way; a fragment carries its own, so anything rendered outside a full page keeps working (ADR-0014). A declaration is _captured from_ a render, not stated ahead of one — what a field needs can depend on its data.
_Avoid_: "the css/js globals" (retired), "registering assets" as a side effect (registration is scoped and returns a value). A **bundle** is a real thing since ADR-0020 — one of six stylesheets grouped by load trigger — but it is still what a render _declares_, not something a build step produces.

**Reserved tag**:
A tag no Content may take because something else already answers to it — `api`, `doc`, `search`, and the routed screens `dashboard`, `admin` and `user`. Only the FIRST path segment competes with a tag, so `dashboard/forms` is as reserved as `dashboard` while handler names in the second segment (`edit`, `raw`, …) are not. Distinct from a _collision_, which is two pieces of Content wanting the same tag and is resolved by suggesting an alternative: a reserved tag is unavailable to everyone, permanently, and the list of them is core's to declare rather than a wiki's to discover. Extends the global uniqueness rule (ADR-0001) from "unique across Content types" to "unique across Content types and routes".
_Avoid_: "system page" (a reserved tag has no Content behind it at all), forbidding by prefix.

**Entry system keys**:
The non-field keys every entry body carries: `tag` (its page tag), `form_id`, `title`, `created_at`, `updated_at`, `status`. Submission artifacts (`antispam`, `valider`, …) are stripped at save, never stored.
_Avoid_: `id_fiche`, `id_typeannonce`, `date_creation_fiche`, `date_maj_fiche`, `statut_fiche` (all renamed).

**Search index**:
The derived table text search reads, in the wiki's own database — never the `body` column, whose JSON envelope matches every page (ADR-0015). Keyed by **tag**, not by revision: only current Content is indexed, so a page's history costs nothing. Every Content type is in it — pages, entries, comments, users, files and forms alike — and a search narrows by content type rather than by which types were indexed. Maintained on write from Content and form events, drained by a command when a form-level change invalidates many rows at once, and rebuildable from scratch: it is a cache of what Content says, never a place anything is only stored.
_Avoid_: "the search table" as a synonym for `SearchManager` (that is the bazar entry _filter_ engine, a different thing), "search column" (retired proposal), calling it a search _engine_ (it is an index; the engine is the database).

**Searchable text**:
What a field contributes to the Search index, **asked of the field** and defaulting from its type the way field roles do (ADR-0012) — not scraped by walking a body. Prose fields give their text with markup and `{{action}}` calls stripped; email, password, file, image and map fields give nothing; enum and checkbox fields give their stored **keys**, and a searched label is translated to keys at query time so relabelling a list reindexes nothing. Derived and system values (`form_id`, timestamps, `stored_filename`, `status`) are as much envelope as the keys are, and are no more searchable.
_Avoid_: "the indexed body" (a body is not indexed, its fields contribute), indexing option labels, a list of excluded field _names_ (the exclusion is a property of the field type).

**ACL bucket**:
One row of the Search index, holding the Searchable text of the fields sharing a single Field ACL expression — public text under the empty expression, and one further row per distinct restriction. Most Content has exactly one. It is what lets restricted text be searchable by the people entitled to it without leaking to anyone else: the wiki's handful of distinct expressions are evaluated once per request, and the passing set filters the query in SQL, so result counts and pagination stay exact.
_Avoid_: post-filtering results in PHP (the retired approach — it makes counts approximate), one document per Content, treating page-level ACL this way (that one is a plain column on the row).

**Avatar**:
An account's face, wherever a person is drawn: its profile picture cropped to a square, or — the normal case, since most accounts never upload one — its first two letters on a colour derived from its name, in black or white by whichever contrasts more (`AvatarService`, `@core/_avatar.twig`). The colour is a function of the name, never stored: the same account is the same colour on every screen forever, and an account that has just been created already has a face. Which field holds the picture is the User form's to say, through the `image` field role rather than by reading `profile_picture` by name.
_Avoid_: "gravatar" (nothing leaves the wiki), "default avatar" (there is no shared placeholder image — the initials ARE the answer), storing the colour.

**Account button**:
What `{{login}}` renders by default (`@core/account-button.twig`): one control in the navigation bar of every page — an icon signed out, your Avatar signed in — linking to `/user`. Signed out it carries the page it was clicked from as `?return=`, which the sign-in that follows redirects to, provided it still points into this wiki (`UrlFormatter::isInternal()`). The sign-in _form_ is a separate template asked for by name (`@core/login-form.twig`) by the screens whose job is signing someone in.
_Avoid_: "login modal" (retired), putting the form in the navbar, trusting `?return=` as given.

**Data source**:
Somewhere outside this wiki that Content is imported from, declared as one entry of the `dataSources` config: which importer reads it (`Rss`, `Imap`, `YesWikiList`, `YesWikiToYesWiki`, or one an extension adds), where it points, which local form it fills, and when it should sync. Importing is a _repeated_ act, not a one-off load: a source is synced again and again, so an importer's real job is deciding what has changed since last time — an entry whose remote side did not move is not rewritten. A source flagged `syncOnMaintenance` syncs itself on the wiki's own housekeeping cadence (`maintenance.after`), with no cron and without the visitor whose page view triggered the sweep waiting for it.
_Avoid_: "feed" (only the RSS importer reads one), "import" for the configuration (an import is a run of a source), calling a sync an "update" of the source (it updates local Content _from_ the source).

**Component**:
Something a webmaster can insert into page content, and the unit the palette offers. A Component knows its label, its icon, its settings and how to write itself — and what it writes is always a `{{tag}}`, which is what makes a Component recognisable in a page it wrote. Deliberately _not_ the same thing as an Action: an Action is a `{{name}}` a PHP class handles, one Component may write more than one of them (`Cards` writes `entrylist` or `syndication`), several may write the same one with different settings, and the two lists have never matched (105 actions against 70 palette entries, in both directions). Declared in PHP by whatever owns it — an Action may declare its own, a Component spanning several declares through a provider of the same interface — never in a data file.
_Avoid_: "action" for a palette entry (`bazarcard` never was one), "widget" (that is the WYSIWYG editor's rendering of one), "block", putting markup syntax in the palette (see **Markup syntax**).

**Markup syntax**:
What the editor toolbars offer and the palette never does: the notation the page body is written in — `**bold**`, `> quote`, `# heading`, `{# comment #}`, and the `:::info … :::` callouts. Reached by formatting text rather than by inserting something, so a toolbar item applied to text that already has it _changes_ it (pressing Information inside a warning retypes the warning) the way a heading level does. The line against a **Component** is mechanical rather than editorial: a Component writes a `{{tag}}`, markup syntax does not — which is what lets a Component be recognised by its tag alone, with no second rule.
_Avoid_: "block" (a callout and a `{{section}}` are both blocks and the distinction is not about that), giving markup syntax a settings rail.

**Presentation**:
A Component that renders a list of **Items** in one shape — Cards, Table, Accordion, Map, Calendar, Timeline. What a webmaster actually chooses between, so it is what the palette shows; _where the items come from_ is its first setting, not a separate palette entry. A Presentation is not tied to one Action: `Cards` is `{{entrylist template="card"}}` over a form and `{{syndication url="…" template="card"}}` over a feed, and looks the same either way.
_Avoid_: "template" for the concept (a template is the Twig file a Presentation renders through), "List" as the name of the accordion Presentation (`liste` is a Content type).

**Source**:
An Action that supplies **Items** to a Presentation rather than rendering anything on its own — `entrylist` (a form's entries), `syndication` (a feed), `comments`, `recentchanges`, `listusers`. A Source has no palette entry: it appears inside the source setting of every Presentation, and declares what it needs to be told (a form id, a feed url). Adding one is a change to the new Action alone — never to the Presentations, which is the whole point of the split. Distinct from a **Data source**, which is external data _imported_ into Content and has nothing to do with rendering.
_Avoid_: "data source" (that is the importer's concept), "provider", giving a Source its own palette entry.

**Item**:
The normalised shape a Presentation renders, one per thing listed: `title`, `subtitle`, `description`, `image`, `url`, `date`, `badge`, `categories`, plus an `id` that keys it and is never shown. **Derived at render time and never stored** — it is a view of Content (or of a feed entry), not a thing in the database. What makes one Presentation work over every Source: a form's fields are mapped onto it (`displayfields`, which already does exactly this), and a feed's fields map straight across. The slots are the ones the presentations already draw, so `badge` is `displayfields`' `floating` renamed — that name said where a card puts it rather than what it is. `image` is a resolved URL, because a Presentation cannot know how to turn an attachment filename into one and the Sources each do it differently.
_Avoid_: "entry" (an entry is Content; an Item is a view of one), "row", "result" (that is search's), `floating` (it was a position, not a thing), storing one.

**Facet**:
A box of checkboxes over one field's values, offered beside (or above) a list so a reader can narrow it — `groups="bf_type"`, laid out by `filterposition`. **Server-side**: checking a box submits the facet form, the server answers with the entries the selection leaves, and htmx swaps the list in — as a column of checkboxes beside the list, or as one chip picker per facet in a row above it (`yw-tags-input.js`, no framework — the checkboxes stay as its no-javascript fallback). The count on each value is over the whole list, not over the current selection, so a second value of a box a reader has already used is still offered. Distinct from `query`, which is the webmaster's fixed restriction on what the list holds; a facet is the reader's, and it is in the URL.
_Avoid_: filtering by hiding rendered items (that is what it used to be, and it only worked for the templates drawing `.bazar-entry`), "filter" alone for the concept (`filtertext` is a different thing, and `query` is too).

**Program**:
The code YesWiki is made of, as one versioned, read-only tree: `src/`, `vendor/`, `templates/`, `themes/`, `javascripts/`, `styles/`. **One Program serves any number of Instances**, which is the farm arrangement, and is what the single binary reuses: it writes its Program out once per version under `YESWIKI_PROGRAM_ROOT` (`~/.local/share/yeswiki/program-4.5.0/` by default, `/opt/yeswiki/…` when an image build states one), not once per wiki. Rebuildable but never optional, which is what separates it from a **Derived artefact**: a missing thumbnail is a shrug, a missing Program is fatal. Nothing writes into it at runtime, so whether it can be written is exactly what decides whether the binary may update itself (ADR-0023). An extension or theme a webmaster installs lands in the Instance's `custom/`, never here.
_Avoid_: "source" (that word is already a palette **Source** and all but a **Data source**), "core" (it is the core/extension split's word), `YESWIKI_SOURCE_DIR` (renamed `YESWIKI_PROGRAM_DIR`), treating it as a cache that may go missing.

**Instance**:
One wiki: its data folders (`custom/`, `files/`, `cache/`, `private/`), its `yeswiki.config.php`, its database, and the `index.php` a webserver points at. Every path **Storage** addresses is relative to it. Which Instance is meant gets resolved once at the edge, from a command's path argument defaulting to the working directory, and is then **stated** as `YESWIKI_INSTANCE_DIR` rather than re-derived from `getcwd()` further in, because under a PHP application server the working directory is not where anybody launched anything.
_Avoid_: "site", "wiki" when the folder is meant, "farm instance" (every wiki is an Instance; a farm is just several of them sharing one **Program**), deriving anything from the folder's name.

**Storage**:
The one way YesWiki's own code touches files. Every read and write goes through it, addressed by an **instance-relative path** — the same strings the code already used (`private/files/abc.webp`, `custom/fonts/nunito/x.woff2`, `cache/container/x.php`). The path decides the Storage tier; there is no second name for it to disagree with, and a path under no known root is an error rather than a guess. Backed by Flysystem, so a tier's home is a local directory or an S3-compatible bucket without any caller knowing which.
_Avoid_: "the filesystem", "disk" (a path may not be on one), passing absolute paths, naming a tier at the call site.

**Storage tier**:
Which of three homes a path has, and therefore what may be done with it. **Public** — served to readers by URL, never through PHP (`custom/`, legacy `files/`, derived images); on object storage its URL points at the bucket or CDN. **Protected** — bytes that exist only behind an access check (`private/files/`, `private/backups/`), streamed by the wiki, never given a URL. **Runtime** — local-only by necessity, because something other than YesWiki reads it as a real file: SQLite, the Loupe search index, the compiled Symfony container, compiled Twig, `custom/extensions/` and the lock files. Configuring object storage for a Runtime path is refused at boot rather than half-working. Source code is not a tier: it is read-only and never written.
_Avoid_: "private/public" as file permissions (Protected is about the access check, not the mode), treating Runtime as a cache that can be pointed anywhere, calling `custom/` private because it sits outside the docroot on some installs.

**Lease**:
A temporary real file on a real disk, for the libraries that cannot take a stream — `ZipArchive`, `Zebra_Image`, `HTMLPurifier::cleanFile`, `getimagesize`. `withLocalCopy()` materialises an object, runs a block, and cleans up; `withLocalTarget()` is the mirror, for producing (or editing) a file and then storing it; `withTemporaryFile()` is the scratch file such a block needs and nobody keeps. On a local tier the first two are a passthrough that copies nothing. The point is that the cost of doing this to a remote object is visible where it is paid.
_Avoid_: stream wrappers (`ZipArchive` ignores them, so the failure is invisible until it is on S3), keeping a materialised copy beyond the block.

**Derived artefact**:
Something Storage may fail to keep without anybody being told: a thumbnail, a cached feed image, a published asset copy. Written with `storeDerived()`, which returns false instead of throwing, because the caller can serve the thing once and make it again next time. Everything else throws on failure — an upload that did not land must never look like one that did.
_Avoid_: treating an upload, a backup or a preset as derived; using `storeDerived()` to make a write quiet.

**Request state**:
Anything whose right value belongs to one visitor's request: the preferred language, a Bazar list index, the panel nesting stack, a form counter, whether the current write came from an import. Under worker mode the PHP process outlives the request, so Request state parked in a global or on a long-lived service becomes the next visitor's. The rule is that it lives in a request-scoped service and nowhere else, checked by a ratchet over `$GLOBALS` writes that may only shrink (ADR-0024). Distinct from **boot state**, which is identical for every request and may stay global: `available_languages`, `installed_languages`, `translations`.
_Avoid_: "session" (a visitor's state _across_ requests, a different problem with a different fix), resetting it between requests rather than removing it, calling the service container "request scope".

**Journal**:
The wiki's own durable record of things that happened, held as rows in its own database and shown at `/admin/logs`. Per wiki always, because a wiki holds credentials to its own database and nothing else (first-class-binary 01) — a shared journal would hand back the blast radius that isolation exists to bound. Distinct from **the log**, which is the stderr stream a process manager collects: everything reaches the log first and unconditionally, and the Journal write is best-effort on top, so a database that is down loses the copy rather than the event.
_Avoid_: calling the Journal "the log" or the log "the journal", the retired `LogDesActionsAdministratives{Ymd}` pages, "history" (a Content's revisions are its history; the Journal records the act, never the content).

**Journal entry**:
One row of the Journal: `at`, `channel`, `level`, `actor`, `action`, `target`, `context`, plus `last_at` and `repeat`. `action` is a dotted code — `content.delete`, `acl.change`, `migration.applied` — rendered through a translation key at read time, never a stored sentence, so a wiki that changes language does not end up with a bilingual trail. An entry is an immutable fact about the past; anything that can stop being true is a **Health check**, not an entry.
_Avoid_: storing a rendered message, a diff or a body (the revision is the content), a stack trace (see **Type-only frame**).

**Channel**:
Which kind of thing a **Journal entry** records: `audit` (a person did something) or `diagnostic` (the runtime hit something). Independent of `level`, which is PSR-3 severity — "refused permission to delete X" is `audit` at `warning`, and collapsing the two would file it under errors. The two channels share a table and nothing else: they have their own retention (`journal_audit_purge_time`, `journal_diagnostic_purge_time`) and only `diagnostic` entries ever collapse.
_Avoid_: using `level` to tell audit from diagnostic, a third channel for advice (that is a **Health check**).

**Type-only frame**:
How a stack frame is written down: `Class::method(string, int, array<3>)` — names and shapes, never values. Safe by construction rather than by a pattern list, which is the point: a redactor that matches `/pass|token/` protects `$password`, misses `$p`, and misses the whole `$_POST` arriving as one `$data` array. Exception messages are stored as authored, because scrubbing free text is that same false promise; not putting a secret in a message is a code rule.
_Avoid_: "redacted" without saying how, name-based redaction, a full trace anywhere.

**Health check**:
A named claim about the wiki's present state, re-derived every time it is asked rather than recorded: an extension is missing, Runtime is nearly full, a bucket is unreachable, an update this instance may actually apply is waiting. Declared by the module that owns it, the way a **Component** is, so no controller has to know about every module. Never a **Journal entry** — a claim that stops being true has no place in an immutable record, which is how a migration's advice used to sit in a wiki page for years after it was acted on.
_Avoid_: logging a finding, a check nobody can act on (an update a farm instance may not apply is not a finding), pass/fail without severity — *broken* and *degraded* are different answers.

## Decisions so far

- No blanket rule on in-place migration of existing installs' data during this rewrite — decided pragmatically per ticket, not deferred wholesale. `acls` was dropped without migrating (reconfigurable via the UI after a reset). `nature` → `pages` (ticket 05) IS migrated in place, since forms are load-bearing content bazar entries depend on to render at all — losing them silently breaks every existing entry, not just access control. Each future ticket should make its own call on whether dropping without migrating is an acceptable reset (like `acls`) or would silently break things (like `nature`), and write a migration when it would.
- `tag` uniqueness is global across all Content types (not scoped per type, not namespaced by prefix). Collisions are avoided at creation time via a suggested-alternative-tag helper.
- Metadata (including ACLs) is versioned along with Content: it rides in the same revisioned `pages` row rather than being mutated in place, so permission history is reconstructable and revertable like content.
- Reverting Content to a prior revision is selective by default (content only); a full revert that also restores that revision's Metadata/ACLs is a separate, explicit action.
- Sensitive `users`-type fields (password hash, tokens) are NOT carved into a separate table — they stay hashed in the same versioned `body` as everything else, protected by Field ACL. Field ACL enforcement applies uniformly to historical revisions, not just the latest.
- Forms keep a stable identifier distinct from their (renameable) `tag`, matching [PR #1333](https://github.com/YesWiki/yeswiki/pull/1333)'s approach. Renaming a form cascades integrity updates to its entries and referencing page content; a former-tag alias may be kept as a triple so previously published sensitive URLs (e.g. ActivityPub actor URLs) keep resolving.
- The pre-existing triples-based `METADATA_PROPERTY` mechanism (non-versioned page facts, e.g. `theme`) is retired in favor of the new versioned `pages.metadata` JSON column — no dual mechanism.
- Extensions live in two places: a shared root `extensions/` (source-scoped, available to every farm instance, instances cannot write into it) and instance-local `custom/extensions/` (instance-scoped, per-instance-only plugins). Reuses the existing lookup precedence already implemented for `custom/tools/{ext}` vs `{sourceDir}/tools/{ext}` in `src/autoload.inc.php` — instance-local shadows shared-root, no new mechanism.
- Which tools get absorbed into core has no fixed criterion — it's periodically re-evaluated with the user community. Confirmed for core (beyond aceditor, attach, bazar, contact, autoupdate, login, rss, security, syndication, tags, templates, toc): qrcode, webhooks, herse, accountactivationbyemail. `lang` and `progressbar` are removed outright, not moved to extensions.
- Bootstrap is dropped from core in favor of a minimal `yw-*`-prefixed CSS/JS design system. Existing themes are expected to be retired, not ported (including `yeswiki-theme-bootstrap3`). New themes are CSS-framework-agnostic.
- From this major release on, core JS is vanilla JS and htmx only — no jQuery, no other JS framework. Piloted first on the `tags` rewrite, then applied everywhere.
- `attach` upload gets one consolidated API-route-backed path, retiring both the legacy page-handler and the vendored `qqFileUploader`. Download enforces the owning page's read ACL by default (a behavior change from today's unauthenticated-by-URL download); a per-file/per-field opt-out flag keeps public embedding possible.
- `tags` gets a real `GET /api/tags` endpoint with pagination and search criteria, replacing "ship every tag in the wiki to every editor" with live search-as-you-type.
- `contact` rewrite is scoped to moving mail-sending to an API route and consolidating through the `Mailer` service (today it bypasses `Mailer` via a bare `send_mail()` call). Today's complete absence of CSRF/spam protection is explicitly out of scope for this pass — deferred to a dedicated later security pass.
- Every file YesWiki's own code touches goes through one `Storage` service (Flysystem), addressed by instance-relative paths whose prefix declares the tier; Public and Protected are pluggable between a local directory and an S3-compatible bucket, Runtime is local by necessity and refuses object storage at boot (ADR-0022).
- `aceditor` keeps the Ace editor library as-is; only its jQuery/Bootstrap wrapper and toolbar are replaced (vanilla JS/htmx, per the core-wide JS rule).
- `tools/templates/` (theme/skin selector, bundled today with ~25 unrelated legacy layout actions including a rights-management action) is split during core absorption rather than moved as one unit: the actual theme/skin-selection logic becomes a core service; the grab-bag actions are triaged individually (delete dead code, fold misplaced ones like `ManageAclsAction` into the relevant absorption e.g. `login`/`security`).
- `security` (today one `SecurityController` bundling hashcash edit-lock, comment spam/captcha, and wiki hibernation status — three unrelated concerns) is likewise split into separate core services during absorption, so the later dedicated security pass has clean seams to extend rather than untangling a bundle first.
- `toc`'s client-side sticky/scroll-spy variant (`tocjs.php`, jQuery + Bootstrap's `.affix()`/`.scrollspy()` JS components, branches on Bootstrap 2 vs 3) is dropped entirely. Only the server-side, CommonMark-based static TOC (`toc.php`) becomes core.
- `login`: all `users`-table access is routed exclusively through `UserManager` as part of this absorption — `tools/login/actions/listusers.php`'s raw SQL bypass is fixed now (found to be the only other direct-SQL access point besides `UserManager` itself), so `UserManager` is the single seam the later users→pages migration needs to touch.
- `accountactivationbyemail` (becoming core): activation status/key move from standalone triples into the `users`-type page's body, protected by Field ACL — same treatment as password hash (ADR-0003). The token also gets an expiry, reusing the `key:issuedAt` + TTL + periodic-purge convention from `UserManager::purgeExpiredPasswordRecoveryKeys()`, which is on `ectoplasme` now (called from `YesWikiRuntime`) and which `AccountActivationService` already follows.
- `autoupdate`: farm-wide core/tools/theme updates target the shared `YESWIKI_PROGRAM_DIR` consistently, triggerable only from an admin-designated instance, not any instance (see ADR-0007).
- `webhooks` (becoming core) stays scoped to comment and Bazar-entry events, as today — not expanded to fire on form or user-account changes despite those now being Content too.
- `syndication`, `rss`, `qrcode`, `herse` move into core with no new decisions needed: `syndication`/`rss` raise no tensions beyond the already-decided stable form-ID (`syndication` uses `id_typeannonce`); `qrcode`'s Bootstrap modal falls under the existing Bootstrap/jQuery-removal rule and its Bazar-backed relation-badge storage is already clean; `herse` is a trivial HTTP Basic Auth gate with no DB/JS/routes.
- Internal identifiers are English everywhere, with no carve-out. This replaces wave two's rule that action names, action parameter names and Twig variable names stayed French: that rule protected stored data in three of its four cases and nothing in the fourth, and the inconsistency cost more than the compatibility it bought. Wiki content written against the old names breaks; a body-rewriting migration comes later, and is deliberately not a precondition. What genuinely cannot move stays put: stored type values (`fiche_bazar`), field type names (`textelong`, `listedatedeb`), page tags (`GererSite`, `RechercheTexte`) and Twig template filenames, all of which are user data rather than identifiers.
- JavaScript is not bundled or concatenated: individual files with versioned, far-future-cacheable paths, on the reasoning that HTTP/2 multiplexing makes request count a weak argument while bundling costs cross-page cache reuse — see ADR-0014's rejected options. **CSS is, by load trigger** (ADR-0020): ~40 stylesheets became six — `yw-core` (always), `yw-admin`, `yw-entries`, `yw-map`, `yw-editor`, `yw-content` — because the argument above is about _request count_, and what was actually wrong was forty files of the same design system with nothing saying which page needed which. The grouping is by what makes a page ask for a bundle, so ADR-0014 keeps meaning what it says: a page with no map and no editor still requests neither. There is no build step — the six are hand-maintained files, and `styles/vendor/**` is untouched.
- Full-text search reads a derived index table in the wiki's own database, not the `body` JSON. Since every body became JSON (ticket 09) a `body LIKE` search matches the JSON envelope itself, so searching for `title` or `content` matches every page. The index is keyed by tag rather than by revision, so it is a table and not a column on the revision-keyed `pages`; it is maintained on write and rebuildable from a command. This also decouples search from body storage, leaving any future native-JSON column to be judged purely on field-query merits. See ADR-0015.
- `pages.body` and `pages.metadata` are **native JSON columns where the dialect has one** — `JSON` on MySQL, `JSONB` on PostgreSQL, `TEXT` on SQLite, behind `SqlDialect::jsonColumnType()`. Judged on the field-query merits ADR-0015 left them to be judged on, and measured rather than argued (`./yeswicli content:body-bench`, 200k entries): on `body`, MySQL halves and PostgreSQL is 8× to 25× faster; on `metadata`, whose `$.acls.read` the read predicate evaluates several times per row on every listing query, PostgreSQL is 62% faster. PostgreSQL's gap is mostly our own doing — a text column forces `jsonExtract()` to emit a regex guard _and_ a text-to-`jsonb` cast per row per extracted field, and the cast costs in proportion to the document, which is why a kilobyte `body` dominates a few-dozen-byte `metadata`. Both columns convert in one ALTER pass because they are on one table: two rebuilds for one decision would be every wiki paying for scheduling. Indexing individual paths was the competing answer and is a complement, not a substitute: it fixes the one path it indexes, and core cannot know which paths a wiki's forms make hot. See ADR-0018.
- Search is one **action** with a route over it, not a route with an action bolted on: `{{search}}` renders a filter form and an htmx-fetched results fragment, and `/search` is a thin controller that runs that action inside the page skeleton. A webmaster embedding `{{search}}` in a page of their own therefore gets exactly what the route gets. `{{newtextsearch}}` and `{{searchform}}` are retired, with a lossy body-rewriting migration and a `RechercheTexte` that becomes `{{search}}`; the top bar gets a plain icon button linking to `/search`.
- Search is core's, and `yeswiki-extension-fulltextsearch` is retired rather than absorbed or kept as a pluggable backend. It is good code, but SEAL ships no relational adapter and Loupe _is_ a SQLite engine, so a MySQL wiki would run a second storage engine as a file outside its own backups — and an index the database cannot see forces ACLs to be post-filtered in PHP, making result counts approximate. Its PDF text extraction is worth having and becomes its own ticket.
- Full-text indexes (MySQL `FULLTEXT`, PostgreSQL `tsvector` + GIN, SQLite FTS5) are built **and exercised** on all three dialects — the compose stack grew a `yeswiki-pg` service and `tests/e2e/reset.sh` takes `YESWIKI_TEST_DRIVER` so the browser suite can be pointed at each. Not just the ones production runs: phpunit runs SQLite only, so a `LIKE` fallback there would mean the suite exercising a query path no real wiki uses — the gap that hid ticket 25's seven defects. The consequences are accepted knowingly: matching becomes token/prefix-based rather than substring, and MySQL's untunable English stopword list and 3-character minimum token diverge from the other two. Stemming is deliberately deferred so the dialects stay comparable.
- Extensions and themes are distributed by `repository.yeswiki.net` and nothing else. The parallel Composer route (`yeswiki-extension`/`yeswiki-theme` package types mapped to `tools/`/`themes/` by `extra.installer-paths`) only ever worked through `oomphinc/composer-installers-extender`, archived and abandoned upstream in May 2026 — and its whole installed base was one theme, `yeswiki/theme-margot`, which is no longer the default. Core now requires no Composer plugin at all. See ADR-0016.
- Importing external data is core's, in the `Content` module: `yeswiki-extension-importer`'s four importers, its `{{adminimporters}}` page, its `importer:sync` command and its api route move in rather than becoming a seventh PSR-4 module — everything an importer writes (entries, lists, forms) already lives in `Content`, so a module of its own would have been a directory, not a boundary (ADR-0013). Extensions keep adding importers of their own: discovery is by service id, with no registry to enrol in. `maintenance.before`/`maintenance.after` exist because of this — core's housekeeping had no hook, and a data source that syncs itself without a cron is the one thing that needed one. See ticket 28.
- The codebase is **eleven modules**, not six: ADR-0013's `Kernel`, `Content`, `Identity`, `Render`, `Search`, `Admin`, plus `Import`, `Contact`, `Files`, `Federation` and `Social` split out of `Content` by ticket 39. `Content` stays the largest at about a quarter of the code, and that is a decision rather than a backlog item — ADR-0011 made a page an entry of the Pages form, so splitting `Bazar` from `Pages` would re-introduce as a module boundary the distinction tickets 10, 11 and 13 were spent removing. The periphery separates because the dependency graph says it does (`Import` and `Contact` had zero edges back from `Content`; the `Files` byte layer has zero edges _into_ it); the core does not, at 308 internal edges with no non-arbitrary cut. Where a module had to be told something, `Content` declares an interface and the module implements it, discovered by DI tag: `ContributesEntryFields` and `AppendsToPageView` for what a view asks of `Social`. The `entry.created`/`updated`/`deleted` events are dispatched by **`EntryManager`**, not `EntryController`, so every write path announces them — they were the form-submission events before, which is why federation had to be called directly instead of subscribing. The consequence is deliberate: **webhooks now fire for imported and migrated entries too**. See ADR-0019.
- Before any implementation starts: `doryphore-dev` is fully reconciled into `ectoplasme` (65 unmerged commits, 12+ security-advisory fixes — see ADR-0008), and every `tools/*/index.php` direct-access guard file is deleted outright (not just its debug tracing code stripped) — direct access to `tools/*` is no longer reachable now that requests funnel through the Symfony kernel/routing, so the guard itself is dead legacy protection.
- YesWiki ships as a **self-contained Linux binary** (x86_64 and arm64) built on FrankenPHP, and that becomes the recommended way to run a wiki. Nothing is dropped: Apache shared hosting stays first-class, because a large share of YesWiki's users are associations on cheap hosting with no shell, and no binary ever reaches them. The binary aims at the people who today cannot deploy at all. It does **not** use FrankenPHP's own `EMBED`, whose app is unpacked into `$TMPDIR/frankenphp_<checksum>` on every boot and never cleaned up, because a `/tmp` sweeper deleting a running wiki's code is ADR-0022's rejected option wearing a different coat. Core carries the tree in `embed.FS` itself and writes it out once, as a **Program**. `setup`, `serve` and `upgrade` are real commands, registered as Caddy commands by a small Go module built in through `XCADDY_ARGS`, with FrankenPHP's `php-server` and `php-cli` left exposed as escape hatches. Bare `yeswiki serve` binds `http://localhost:8080` and `--domain` opts into public binding with automatic HTTPS, so no first run touches a privileged port or an ACME server by accident. See ADR-0023.
- The binary **replaces itself**, fetching from `repository.yeswiki.net` and verifying an ed25519 signature against a public key compiled in, which extends ADR-0016's "that host and nothing else" to core rather than adding GitHub as a second thing a wiki trusts, and keeps the `yeswiki_repository` override working for a private mirror. Whether it may do so is decided by **whether the Program root is writable**, not by sniffing for a container: a laptop's `~/.local/share` is writable and upgrades; an image's read-only `/opt/yeswiki` refuses and names the path. Fetch/verify/swap is separable from migrate, so an image deployment runs migrations as a job. This is the one place a binary is not simply better: `isDesignatedUpdateInstance()` already refuses every farm instance, and the same check would have refused the binary for entirely the wrong reason (see ADR-0007's amendment).
- Everything the binary might ever need is **compiled in, because nothing can be added afterwards**: a fully static musl build cannot load a PHP extension, ever. That means the `composer.json` list plus `ext-openssl`, which `HttpSignatureService` and `KeyPairGenerator` have always used and nobody declared, plus all three PDO drivers, plus opcache, plus `ext-imap` so ticket 28's IMAP importer still exists. A few megabytes against a feature being permanently absent is not a close call, and the undeclared `openssl` says the manifest was never trustworthy as one. SQLite runs in WAL mode with a busy timeout, set at `setup`, because worker mode puts several threads on one file.
- The binary runs FrankenPHP in **worker mode**, so **Request state** stops being allowed to live in a global. Classic mode was the safe answer and was rejected deliberately: worker mode is the reason to reach for FrankenPHP at all, and the cost is bounded and countable. There are 193 `$GLOBALS[...]` sites, and they are not one problem. 78 are the service container, which injection replaces the way wave two replaced the service locator; `available_languages`, `installed_languages` and `translations` are boot state and stay. What is left is genuine Request state with genuine leaks: `ContactAction` increments `nbactionmail` behind an `isset()` so the counter climbs for the life of the process, `EntryListAction:1760` does the same to `_BAZAR_['listindex']` which feeds DOM ids, `PanelAction` pushes and pops `check_{tag}['panel_shape']` as a stack that two visitors on one page share and a request ending mid-stack hands on dirty, and `FormPropertiesService` and `SubscribeField` branch on `_BAZAR_['provenance'] === 'import'`, which left set makes every later request in that worker believe it is an import and skip subscription mail. Those are **eliminated, not reset**: a reset routine is a list somebody has to remember to extend, and the day it falls behind the symptom is cross-visitor bleed rather than an error. A PHPStan rule over `$GLOBALS` writes ratchets to zero the way ticket 40's baseline does, so worker safety becomes a property of the code instead of something that was true once. See ADR-0024.
- The installer stops being a controller. The steps in `InstallationController::install()` move into a service with no HTTP in them, and get two front ends: the existing web wizard and an interactive CLI. `InstallationController` is precisely the route-less controller doing service work that wave two spent tickets 03 and 04 on, and two installers that can drift is the defect this rewrite has removed everywhere else. Backup import comes along for free, since `importBackup()` is one of the steps.
- A wiki runs at **one replica**. The binary suits a container well and ADR-0022's tiers were written for exactly that, but YesWiki is not horizontally scalable yet and saying so is cheaper than finding out: sessions are files on the node's own disk (`YesWikiInit.php:613`), the hashcash secret is `cache/hashcash.key` and Runtime-tier by design, so one node issues a puzzle another rejects on save; `cache/maintenance.lock` is local, so N nodes run N housekeeping sweeps and therefore N syncs of the same **Data source** since ticket 28 hung importers off `maintenance.after`; and `custom/extensions/` is Runtime, so an extension installs onto whichever node served the click. Four separate pieces of work, worth doing on their own terms, deferred to their own spec rather than doubling this one.
- The **browser suite is a local gate, not a CI job**. GitHub Actions minutes are not free and there is no budget for a Playwright matrix, so `tests/e2e/` runs on the compose stack on a maintainer's machine (`docker compose -f docker-compose.yml -f docker-compose-test.yml up -d --build`, then `tests/e2e/reset.sh` and `npx playwright test` once per driver), and is expected green on MySQL and PostgreSQL before a release. CI carries the phpunit matrix instead: MySQL 8.3/8.4/8.5, SQLite, PostgreSQL and an S3 leg, each with a real install and `./yeswicli migrate` before the suite. This is a cost decision, not an oversight; adding a browser job to CI would undo it. A spec that only passes because of what happens to be in the author's `custom/` or database is the failure mode this arrangement invites, and two have been found that way already (`custom-templates.spec.ts`, `EntryEventsTest`).
- **The Program and the Instance are stated, not inferred** (ticket 43). `bootstrap_paths.php` resolves each of `YESWIKI_PROGRAM_DIR` and `YESWIKI_INSTANCE_DIR` from an already-defined constant, then the environment, then a fallback, and refuses a value that does not resolve to a directory. `YESWIKI_SOURCE_DIR` is gone with no compatibility constant. This is what an application server needs: `getcwd()` is the docroot under php-fpm and is wherever the unit file was under a worker. **Which root owns a file is now a question every path has to answer** -- `composer.json` lives in the Program and `index.php` in the Instance, and looking for both in the working directory is why no farm instance has ever been able to take a backup.
- **`binary/` is the Go module the single binary is built from** (single-binary 01). Three layers: `internal/program` decides where the Program goes and writes it, `internal/commands` is what `setup` and `serve` do with the PHP runtime behind an interface, and `main.go` registers the two Caddy commands. The split is so that `go test ./...` runs without a PHP build -- the FrankenPHP dependency is one import, and everything that decides anything sits on the other side of it. `serve` already serves an Instance; the Caddyfile it deserves is ticket 03. `binary/build-static.sh` (ticket 02) reads the extension set out of `composer.json` rather than carrying a list, so what a static binary compiles in -- and can never add later -- cannot drift from the audit; `binary/check-binary.sh` asserts the built artefact against the same manifest, and release.yml builds x86_64 and arm64 natively. One extension cannot be there at all: **`ext-imap` is not thread safe and FrankenPHP is ZTS**, so the IMAP importer is a php-fpm feature and a wiki that imports a mailbox cannot move to the binary (ADR-0023).
- **Installing is a service, not a controller** (ticket 44). `InstallationService` holds the seven steps with no HTTP in them; `InstallationController` is 161 lines of request handling, and `core:install` is the same steps in a terminal -- interactive by default, scriptable with `--no-interaction`, and able to restore a backup without a browser. The browser suite installs itself that way now. Installed twice on the same driver, the web wizard and the command write configuration files that differ only in what the operator typed differently.
- **The gate on updates is per package, not per instance** (ticket 46). ADR-0007 exists because a core update mutates the shared Program; a theme or an extension installs into the Instance's own `custom/`, so refusing those was the check asking the wrong question. `PackageTheme` targets `custom/themes/` on a farm the way `PackageTool` already targeted `custom/extensions/`, and no farm instance could install a theme before.
- **`composer.json` names every extension core calls into**, checked by `ExtensionManifestTest` rather than maintained by hand (ticket 47). Eight were missing, `ext-openssl` -- which signs every ActivityPub request -- among them; nobody noticed because every distribution ships them. That stops being free with the static binary, which contains exactly what it was compiled with and can never load another, so an undeclared extension is a feature that silently does not exist. Three more come only from the vendor -- `libxml`, `xml`, `xmlreader` -- and are declared too, because "what core calls" and "what a server needs" are different questions and the person provisioning the host is answering the second. The test also holds the phpunit workflow's `extensions:` line and INSTALL.md's list to what `require` names, because a comment claiming they agree is what let them drift.
- The **PHPStan baseline is empty** (ticket 40, 3,398 suppressions to 0), and `PhpstanBaselineRatchetTest` asserts that. There is no `make analyse-baseline` any more: regenerating a baseline can only hide a fresh error, so a report is either a defect to fix or one of the six documented `ignoreErrors` in `phpstan/phpstan.neon`, each with its reason written beside it. To work on a few files without the rest of the project in the way, point `phpstan/onefile.neon` at them. The value of the burn-down was never the number: typing a signature makes the analyser read the body properly, and that is what surfaced a migration that would have blanked entry bodies, group membership edits dropped without an error, an upgrade that extracted over the filesystem root, and a dozen branches holding features that silently were not there.
